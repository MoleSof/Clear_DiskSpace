$sudo apt autoclean
$sudo apt autopurge
####What the fuck is wrong with APT?
sudo dpkg --configure -a
$sudo apt install -f
$sudo apt install --fix-broken
sudo dpkg --configure -a
####What the fuck is wrong with APT?
$sudo apt install -f
$sudo apt install --fix-broken
$sudo apt autoremove
$sudo apt autopurge
$sudo apt autoclean
$sudo apt clean
$sudo apt held
$sudo apt purge
####add -y and complete with error but print
